<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.yy">

    <application
        android:name=".app.PharmacyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".SaleReportDetailActivity"></activity>
        <activity android:name=".SaleReportAddActivity" />
        <activity android:name=".SaleDetailActivity" />
        <activity android:name=".SaleAddActivity" />
        <activity android:name=".SupplierDetailActivity" />
        <activity android:name=".SupplierAddActivity" />
        <activity android:name=".CustomerDetailActivity" />
        <activity android:name=".CustomerAddActivity" />
        <activity android:name=".MedicineDetailActivity" />
        <activity android:name=".MedicineAddActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>